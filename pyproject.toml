[project]
name = "hexlet-code"
version = "0.1.0"
description = "A minimal Flask web application with a /ping endpoint"
readme = "README.md"
authors = [
    { name = "Mihail Pirko", email = "pirkomihail85@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "flask>=3.0.0",
    "gunicorn>=23.0.0",
    "sentry-sdk[flask]>=2.0.0",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "ruff>=0.16.3",
]
